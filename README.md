# ⚡ Go Concurrent Processor

Мини-проект на Go, демонстрирующий практическое владение конкурентностью:

- Goroutines
- Channels
- Worker Pool
- Fan-out / Fan-in
- Context cancellation
- Graceful shutdown
- sync.WaitGroup
- Корректное управление жизненным циклом каналов

Проект минималистичный по объёму, но реализован в production-style формате.

---

## 🧠 Что демонстрирует проект

Это не просто пример с горутинами.  
Здесь показано:

- Ограничение параллелизма (bounded worker pool)
- Безопасная отмена выполнения через `context`
- Отсутствие утечек горутин
- Правильное закрытие каналов
- Работа через `select`
- Координация потоков выполнения
- Корректная синхронизация через `sync.WaitGroup`

---

## 🏗 Архитектура

Программа состоит из трёх логических частей:

### main.go
- Создание `context` с таймаутом
- Запуск пула воркеров
- Генерация задач
- Сбор результатов (fan-in)
- Корректное завершение работы

### worker.go
- Получение задач из канала
- Обработка задачи
- Отправка результата
- Завершение при отмене контекста

### task.go
- Определение структуры задачи
- Бизнес-логика обработки

---

## 📂 Структура проекта
```
go-concurrent-processor/
├── main.go
├── worker.go
└── task.go
```

Все файлы находятся в одном пакете `main`.

---

## 🚀 Принцип работы

1. Запускается фиксированное количество воркеров.
2. Задачи отправляются в канал `tasks`.
3. Воркеры параллельно обрабатывают задачи.
4. Результаты отправляются в канал `results`.
5. При истечении таймаута:
   - Контекст отменяется
   - Воркеры корректно завершаются
   - Каналы закрываются
   - Программа завершает работу без утечек

---

## ▶️ Запуск проекта

```
go run .
```